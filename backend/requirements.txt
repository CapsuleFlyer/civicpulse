# Runtime dependencies, pinned. An unpinned dependency is an unreproducible
# build: pip would happily resolve a different tree on the CI runner than the
# one that passed on your laptop.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
redis==5.2.1
httpx==0.28.1
prometheus-client==0.21.1
